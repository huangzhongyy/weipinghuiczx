<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace就是接口的全路径 -->
<mapper namespace="com.cssl.dao.ShangJiaDao">
  <select id="login" resultType="shanjia" >
      select * from shanjia where s_name = #{sj.s_name} and s_pwd = #{sj.s_pwd}
  </select>
</mapper>